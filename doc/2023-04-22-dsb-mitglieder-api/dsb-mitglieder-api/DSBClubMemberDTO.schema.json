{
    "$schema": "https://json-schema.org/draft/2020-12/schema#",
    "type": "object",
    "properties": {
        "uuid": {"type": "string", "format": "uuid"},
        "club-uuid": {"type": "string", "format": "uuid"},
        "person-uuid": {"type": "string", "format": "uuid"},
        "member-from": {"type": "string", "format": "date"},
        "member-until": {"type": "string", "format": "date"},
        "licence-state": {"type": "string"},
        "licence-valid-from": {"type": "string", "format": "date"},
        "licence-valid-until": {"type": "string", "format": "date"}
    },
    "required": [
        "uuid",
        "club-uuid",
        "person-uuid"
    ]
}