{
    "$schema": "https://json-schema.org/draft/2020-12/schema#",
    "type": "object",
    "title": "DSBPersonDTO_",
    "description": "Vereinsfunktionärsdaten, die an REGIONAL AUTORISIERTE endpoints geliefert werden.",
    "$comment": "protected properties",
    "allOf": [
        {"$ref": "DSBPersonDTO.schema.json"}
    ],
    "properties": {
        "address-uuid": {"type": "string", "format": "uuid"},
        "birthdate": {"type": "string", "format": "date"},
        "birthplace": {"type": "string"},
        "birthname": {"type": "string"},
        "remarks": {"type": "string"},
        "dead": {"type": "integer"},
	"privacy-state: {"type": "integer"}
    }
}