{
    "$schema": "https://json-schema.org/draft/2020-12/schema#",
    "title": "DSBRegionDTO",
    "description": "Regionsdaten, die an ALLE endpoints geliefert werden",
    "type": "object",
    "$comment": "public properties",
    "properties": {
        "uuid": {"type": "string", "format": "uuid"},
        "name": {"type": "string"},
        "nickname": {"type": "string"},
        "pattern": {"type": "string"},
        "parent-region-uuid": {"type": "string", "format": "uuid"}
    },
    "required": [
        "uuid",
        "name",
        "nickname",
        "pattern"
    ]
}